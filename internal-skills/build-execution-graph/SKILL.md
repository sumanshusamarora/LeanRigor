# Build Execution Graph

Create executable tasks with objectives, read sets, write sets, dependencies, validation commands, and completion criteria. Reject cycles. Group independent tasks into waves and flag write conflicts before execution.
