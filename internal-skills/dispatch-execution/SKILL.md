# Dispatch Execution

Run tasks in dependency order. Parallelise only tasks with compatible file ownership and stable contracts. Give each agent only relevant context. Require scope expansion before modifying unassigned files.
