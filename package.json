{
  "name": "leanrigor",
  "version": "0.1.0-draft",
  "private": true,
  "description": "Adaptive rigor and portable model routing for AI coding agents.",
  "type": "module",
  "bin": { "leanrigor": "dist/cli/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "check": "npm run typecheck && npm test"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "commander": "^14.0.0", "fast-glob": "^3.3.3", "picocolors": "^1.1.1", "zod": "^4.0.0" },
  "devDependencies": { "@types/node": "^24.0.0", "typescript": "^5.9.0", "vitest": "^3.2.0" }
}
